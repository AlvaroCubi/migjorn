/target
**/*.rs.bk
Cargo.lock.orig

# Python
__pycache__/
*.pyc
*.pyd
.pytest_cache/
*.egg-info/
# uv writes this when it treats the package dir as a project; the example's
# `uv run --with` commands resolve fresh and don't need a committed lock.
uv.lock
