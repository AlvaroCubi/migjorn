[package]
name = "crunchy-syntax"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
rustc-hash = "2"

[dev-dependencies]
insta = { version = "1", features = ["glob"] }
